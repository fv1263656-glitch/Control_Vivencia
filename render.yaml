services:
  - type: web
    name: control-vivencia
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: control-vivencia-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 10000
